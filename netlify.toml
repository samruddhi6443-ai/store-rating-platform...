[build]
  base = "client"
  command = "npm ci --legacy-peer-deps && chmod +x ./node_modules/.bin/* && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10"
